// take all panel data and actually create the panels and mount them into the scene

import * as THREE from 'three';

function MountPanels (navProg, group, fullPoints, skinnyPoints, panelMidpoint, panelContent, skinnyContent, layout) {
    
}

export default MountPanels;