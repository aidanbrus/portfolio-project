// for creating the gantry
import * as THREE from 'three';

function makeGantry (center, axis, length, width, height) {
    // creating the points for each box
    if (axis = 'x') {
        
    }

    beamCurve = new THREE.CatmullRomCurve3( [
        new THREE.Vector3( 0, 0, 0),
        center,
        new THREE.Vector3( 0, 0, 0)
    ] )
}